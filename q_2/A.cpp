#include <bits/stdc++.h>

using namespace std;

int main(){

string s;
getline(cin, s);

s += ' ';

for(int i = 0; i < s.size(); i++){
    string a;
    int cnt = 0; 
    if(s[i] != ' '){
        a[i] += s[i];
        cnt++;
    }
}

return 0;
}